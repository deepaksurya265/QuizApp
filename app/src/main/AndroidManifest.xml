<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wiates.quizapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".User.ViewLeaderboard"></activity>
        <activity android:name=".User.LeaderboardTestList" />
        <activity android:name=".User.ViewParticipation" />
        <activity android:name=".Admin.ViewAttempts" />
        <activity android:name=".User.AttemptView" />
        <activity android:name=".User.ViewAttempts" />
        <activity android:name=".User.SuccessfullySubmittedActivity" />
        <activity android:name=".User.ViewQuestions" />
        <activity android:name=".User.TestInstructions" />
        <activity android:name=".User.ListTestsActivity" />
        <activity android:name=".Admin.NewQuestionEntryActivity" />
        <activity android:name=".Admin.ListTestsActivity" />
        <activity android:name=".Admin.NewTestCreating" />
        <activity android:name=".Admin.HomeScreenAdmin" />
        <activity android:name=".User.HomeScreenStudent" />
        <activity android:name=".SignupScreen" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginScreen" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>